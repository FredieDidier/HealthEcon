% =============================================================================
% supplement.tex — Supplemental Appendix for "Born on Schedule", compiled as a
% standalone online-supplementary document. It \inputs the shared sup_appendix.tex
% body (Appendix C: additional descriptive figures and tables; Appendix D:
% robustness, validation, and inference; Appendix E: the policy record).
%
% Cross-references to the main paper (equations, tables) are resolved from
% paper.aux via the xr package, so compile paper.tex FIRST, then this file.
% Compile order:  pdflatex paper ; pdflatex supplement ; pdflatex supplement
% =============================================================================
\documentclass[12pt]{article}

\usepackage[margin=1in]{geometry}
\usepackage{booktabs}
\usepackage{float}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage[round]{natbib}
\usepackage{caption}
\captionsetup{font=small,labelfont=bf}
\usepackage{rotating}
\usepackage{xcolor}
\definecolor{citegreen}{RGB}{0,120,60}    % house green, as in HomeOfficePNAD
\usepackage[colorlinks=true,citecolor=citegreen,urlcolor=citegreen,linkcolor=citegreen]{hyperref}
\usepackage{xr}                 % external cross-references to the main paper
\externaldocument{paper}        % resolves \eqref{eq:...} and \ref{tab:...} etc.

% Figure-notes macro, identical to paper.tex (justified, full width).
% Figure notes must match the table notes: the same justified block at the FULL
% width of the text column (NOTE_OPEN / NOTE_CLOSE in analysis/code/00_utils.R).
% This is the Monasterio form. Do not narrow it, do not wrap it in \centerline,
% and do not re-add \centering; see the note-style history in CLAUDE.md.
\newcommand{\fignotes}[1]{%
  \par\vspace{1pt}%
  \begin{minipage}{\linewidth}\footnotesize%
  \textit{Notes:} #1%
  \end{minipage}}

\title{Supplementary Appendix\\
\large Born on Schedule: Fees, Supply-Side Scheduling, and Cesarean Delivery
in Brazil}
\author{}
\date{}

\begin{document}
\maketitle
\thispagestyle{empty}

% Number appendix tables/figures by their appendix section (C.1, D.1 ...), and
% start the section counter at C so the shared body reads as Appendix C, D, and E.
\appendix
\setcounter{section}{2}
\numberwithin{table}{section}
\numberwithin{figure}{section}

\input{sup_appendix}

% The supplement now carries citations of its own (the multiple-testing
% procedures), so it needs its own reference list. Compile with:
%   pdflatex supplement ; bibtex supplement ; pdflatex supplement (x2)
\bibliographystyle{plainnat-rev}
\bibliography{refs}

\end{document}
